import MainHeader from "./main-header";
import TopHeader from "./top-header";

const Header = () => {
    return (
        <header >
            <TopHeader />
            <MainHeader />
        </header>
    );
};

export default Header;
